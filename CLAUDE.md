# GHL ONE — Company Operating System for GHL India Ventures

Next.js 16 (App Router, `src/`), React 19, Tailwind v4, Supabase (project `rarlrohsybktcheyrhiv`).
Positioning: *One Company. One Workspace. One Source of Truth.*

## Conventions (read before writing code)
- **Auth/session**: server pages call `getSession()` from `@/lib/session` (redirects to /login or /pending). Client components use `useSession()` from `@/components/providers/SessionProvider` → `{ profile, departments, people }`.
- **Supabase**: server → `createClient()` from `@/lib/supabase/server`; client → `createClient()` from `@/lib/supabase/client`. Types in `@/lib/database.types` (generated). Row/enum aliases + label maps + helpers in `@/lib/utils` (`Task`, `Project`, `STATUS_LABEL`, `PRIORITY_TONE`, `fmtDate`, `relDate`, `ago`, `isManagerPlus`…).
- **UI primitives**: `@/components/ui` — `Button`, `Pill`, `Avatar`, `AvatarStack`, `Card`, `CardHeader`, `Input`, `Textarea`, `Select`, `Field`, `SearchInput`, `Modal` (`side` for drawer), `Tabs`, `EmptyState`, `Spinner`, `Skeleton`, `Kbd`, `Stat`, `Progress`, `Menu`/`MenuItem`, `PageHeader`, `useToast()`.
- **Pickers**: `@/components/pickers` — `PersonPicker`, `DepartmentPicker`, `PriorityPicker`, `StatusPicker`, `ClassificationPicker`, `ProjectPicker`.
- **Task bits**: `@/components/tasks/TaskBits` — `StatusPill`, `PriorityPill`, `WaitingPill`, `DueLabel`, `PersonChip`, `TaskRow`. `@/components/tasks/QuickTaskForm` creates tasks.
- **Styling**: CSS tokens in `globals.css` (`var(--bg-elev)`, `var(--line)`, `var(--fg-muted)`, tones `.tone-*`, `.card`, `.btn`, `.input`, `.pill`, `.page`, `.h1/.h2/.h3`, `.eyebrow`, `.text-muted`). Golden-ratio spacing `--s1..--s7`. Dark mode = `.dark` on `<html>`; use tokens, never hard-coded greys. Mobile first: no horizontal overflow; bottom nav is 56px on <lg.
- **Routes** live under `src/app/(app)/…` (inside the shell). Auth pages outside: `/login`, `/pending`, `/auth/*`. Proxy at `src/proxy.ts` protects everything else.
- **Data rules** are enforced in Postgres (RLS + triggers). Notifications, task history, audit logs, calendar entries and project channels are created by triggers — do not duplicate in the client. RPCs: `search_all`, `company_pulse`, `department_health`, `workload`, `open_dm`, `mark_channel_read`, `my_unread_counts`, `task_from_message`, `create_project_from_template`, `create_delegation`, `related_to`.
- Roles: super_admin > director > executive > department_head > manager > team_lead > employee > intern > consultant > vendor > guest. Helpers `isAdminRole`, `isManagerPlus`, `isLeadPlus`, `isInternal`.
- Never run `next build` while `next dev` is running. Verify in the browser, then commit → push → deploy.

## Intelligence (Phase 2)
- Server-only AI layer in `src/lib/ai/` (`client.ts` = Anthropic SDK wrapper `complete()` / `extract()` + usage logging; `context.ts` = permission-safe context builders that ALWAYS use the caller's RLS-scoped Supabase client; `prompts.ts` = stable cacheable system prompts; `route.ts` = `withAI()` auth/error wrapper + summary cache helpers). Routes under `src/app/api/ai/*` (ask, brief, project-summary, meeting-extract, extract-tasks, delegate-parse, catch-up, risks, search, status). Client code uses `callAI()` and types from `@/lib/ai/types` and must handle `.disabled` (no `ANTHROPIC_API_KEY`).
- Model: `claude-opus-5` (override with `AI_MODEL`). Cheap routes use `effort: "low"`. The AI never executes actions — it proposes; humans confirm in the UI.
- Cached outputs live in `ai_summaries`; usage in `ai_usage` (admin-visible).

## Automation (Phase 3)
- Engine lives in Postgres (`0006_automation.sql`): `automations` (WHEN trigger_type + trigger_config → IF conditions → THEN actions; see `run_automation_action` for action JSON) fired by AFTER triggers via `fire_automations()` with a depth guard (max 2); `automation_runs` log; `schedule` triggers and the escalation ladder (`escalation_rules`) run on pg_cron (`run_scheduled_automations` every 5 min, `run_escalations` every 15 min, digests daily/weekly 08:30 IST). Recurring tasks: `tasks.recurrence` → next occurrence spawned on completion. Handoffs: `handoffs` table + trigger moves/assigns the task. Outgoing webhooks/Slack via `pg_net`; incoming via `/api/hooks/{token}` → `ingest_webhook()`; personal ICS feed via `/api/calendar/{token}.ics` → `calendar_feed()`.
- Template variables for automation text: `{{title}} {{assignee_name}} {{owner_name}} {{project_name}} {{department_name}} {{due}} {{link}} {{status}} {{priority}} {{actor_name}}`.

## Migrations
`supabase/migrations/*.sql` are the source of truth and are applied to the remote project via the Supabase MCP (`apply_migration`). Keep them additive.

## Employee & Team OS (Phase 4)
- Schema: `0008_workforce_enums.sql`, `0009_workforce_core.sql` (attendance, leave types/balances, shifts, focus/time entries, admin roles, `profiles_private`), `0010_collab_governance.sql` (groups: `channels.owner_id/co_owner_id/purpose/visibility/archive_at/archived/settings/department_ids`, `channel_members.expires_at/invited_by/invite_reason`; help desk: `service_catalog`, `help_requests` (status new→accepted→working→waiting→completed/declined; accepting opens a `help` channel), departments `status/on_duty_user_id/escalation_matrix`; governance: `access_requests` → `access_grants` (feed `can_view_project`/files/channels), `feature_flags`, `security_events`).
- RPCs: attendance `clock(p_kind,p_mode,p_note,p_location,p_source)`, `attendance_board(p_day)`, `my_attendance(p_from,p_to)`, `correct_attendance`, `roster(p_from,p_to,p_dept)`, `start_focus`, `stop_focus`, `timesheet_suggestions(p_day)`, `activity_timeline`; leave `my_leave_balances(p_user)`, `leave_impact`, `prepare_handover`; collaboration `bring_in(p_channel,p_user,p_reason,p_expires_at)`, `invite_department`, `convert_channel_to_project`, `department_availability()`; governance `has_grant`, `revoke_grant`, `revoke_everywhere`, `explain_access(p_user,p_type,p_id)`, `who_can_see(p_type,p_id)`, `view_as(p_user)`, `company_now()`, `since_last_visit(p_since)`, `collaboration_map()`, `has_admin_perm(perm)`, `is_primary_admin()`.
- Principles (non-negotiable): no surveillance (no keystrokes/webcam/mic/screenshots/continuous location; location only on explicit clock-in with consent); everything an employee's data shows is visible to the employee too (Privacy Center); nothing important is visible/executable without the right permission — show **Request access** (creates `access_requests`) instead of a bare "Access denied"; Super Admin actions are audited; AI proposes, humans confirm; respect quiet hours/DND.
- Activity lights: `@/components/providers/ActivityProvider` — `<Blink zone="nav:/help" />`, `<Blink zones={[…]} />`, `useActivity(zone)`, `useSeen(zone)`. Zones: `nav:<href>`, `dept:<id>`, `channel:<id>`, `project:<id>`, `task:<id>`, `user:<id>`, `team:<id>`, `help:<id>`. Put a `<Blink>` on every list row / card / tab that represents one of these entities, and call `useSeen` in its detail view. Events are RLS-filtered by Supabase Realtime and role-scoped in the provider — never blink something the viewer cannot open.
- Routes: `/attendance`, `/leave`, `/common` (GHL Common hub), `/help` (+ `/help/[id]`), department portals at `/departments/[slug]`, Super Admin console under `/admin` (tabs). Nav in `src/components/shell/nav.ts`.

## Wave 2 schema (AI Buddy + HR ops)
- `0011_ai_buddy.sql`: `ai_assistants` (key general|it|sales|support|design|content|hr|manager|department_head|new_joiner; `action_level` 1–6, `data_scopes`, `daily_limit`, `department_ids`), `ai_knowledge` (approved department knowledge: status draft→approved by `ai_knowledge_owners`/department head/AI admin, `review_at` expiry, tsvector `search`), `ai_feedback` (helpful|not_helpful|incorrect|outdated|unsafe → routed to knowledge owner), `ai_actions` (proposed→confirmed→performed log), `ai_memory` (personal, per user), `learning_interests`; RPCs `search_knowledge(p_q,p_department,p_limit)`, `find_experts(p_q,p_limit)`, `restricted_hits(p_q)` (matches the caller may NOT see → "found but restricted" + Request access), `blocker_chain(p_task)`, `department_brief(p_department)`, `ai_requests_today()`, `is_knowledge_owner(d)`.
- `0012_hr_ops.sql` (+`0013_wave2_fixes.sql`): executable SOP workflows `workflow_templates` (seeded onboarding/offboarding/transfer/promotion/incident; steps JSON with owner hr|it|admin|manager|employee|department_head|primary_admin|department:<slug>|user:<id>, depends_on, due_days) → `start_workflow(key, subject, ctx, label)` creates `workflow_runs`/`workflow_steps` and a task per ready step; completing the task releases dependents (`skip_workflow_step`); onboarding starts automatically when a profile becomes active. `employee_transfers` + `apply_transfer(id)`, `role_changes` + `apply_role_change(id)`, `probation_reviews` (cron reminders 7 days before `profiles.probation_ends_on`), `transfer_work(from,to,reason)`, `offboard_user(user, transfer_to, reason, last_day)`; `assets`/`asset_assignments`/`asset_requests` (routed to manager → IT); `employee_documents` (bucket `hr`, path `<user_id>/…`); GHL Academy `courses`/`lessons` (kind text|video|document|sop|quiz with `quiz` JSON `[{q,options[],answer}]`)/`enrollments`/`lesson_progress` (progress + completion computed by trigger); `skill_endorsements` (verified when endorsed by manager/HR); `goals` (level company|department|team|employee, `parent_id`, `goal_tasks`); `one_on_ones`; `feedback` (visibility recipient|manager|hr); `kudos` (public → posted to #wins); `suggestions` (+ `suggestions_view` hides anonymous authors); `job_openings`/`internal_applications`/`candidates`/`interviews` + `hire_candidate(candidate, role, designation, manager, department)` → invite; `mentorships`; `shift_handovers` + `prepare_shift_handover(dept)`; `incidents` + `start_war_room(title, severity, department, summary)` (emergency channel + critical task + alerts) and `urgent_assistance(kind it|hr|management|security|operations, message)`; `claim_help_request(id)`; helpers `is_hr()`, `is_manager_of(u)`.
- Routes planned: `/academy` (+`/academy/[id]`), `/goals`, `/jobs`, `/help/incidents/[id]`, HR console at `/admin?tab=hr` (views: people, onboarding, transfers, probation, assets, documents, ats) and `/admin?tab=workflows`.

## GHL Buddy (AI companion)
- Backend: `POST /api/ai/buddy` (`src/app/api/ai/buddy/route.ts`) built on `src/lib/ai/buddy.ts` (assistant selection by department/role/tenure, RLS-scoped tools, page scope, proposals, confidence parsing) and `src/lib/ai/buddyPrompts.ts` (system prompt + mode instructions + tones). Request/response contract in `src/lib/ai/types.ts` (`BuddyRequest`, `BuddyResponse`, `BuddyProposal` with 14 action kinds, `BuddyMode`, `BuddyAttachment` image/pdf/text, `BuddyScope`). Every answer carries `confidence`, `context` (what it used), `restricted` hits (→ Request access), `proposals` (human-confirmed), `handoff` (human routes) and `suggestions`.
- Modes: chat, stuck (I'M STUCK), debug, incident, practice, check, prepare, explain_simple, explain_technical, breakdown, who_can_help, what_next, looking_at, why_blocked, translate, draft, brief. Action levels from `ai_assistants.action_level`: ≤2 answer/suggest only, 3 drafts, ≥4 confirmable proposals; 6 = automatic only via automations.
- Front-end: `BuddyPanel` (`src/components/ai/BuddyPanel.tsx`, opened by `openBuddy({mode, message, scope})` from `src/components/ai/buddyStore.ts`), `BuddyProposals` (confirm handlers = plain Supabase writes under RLS; log outcome in `ai_actions`), `BuddyQuickActions` (universal buttons on task/project/chat/file/help/meeting pages), `@GHLBuddy` in chat (reply posted as the asker's message prefixed `🤖 **GHL Buddy** · `), `BuddyHomeCard` nudges, `InboxDigest` (`/api/ai/inbox`), knowledge pages under `/wiki/knowledge`, AI control center in `/admin?tab=ai`.
- Rules: the AI never executes; it proposes. Never reveal restricted content. No people-judgement outputs. Always keep a human path (chat / invite / department help / ticket) next to every answer.

## Phase 5 — Employee control, screen governance, org intelligence (schema 0017–0018)
- `profiles.status` (active|probation|contract|intern|notice_period|suspended|inactive|exited), `frozen`, `functional_manager_id`, `contract_ends_on`, `job_scope`, `config_incomplete`; history in `manager_history` / `role_history` (set `ghl.change_reason` via RPCs). `is_active_member()` now excludes frozen people and frozen departments.
- **System roles** (`system_roles`, seeded 17; `user_roles` with `acting`/`expires_at`), `role_defaults` per hierarchy level, `departments.default_permissions/default_screens`, `permission_overrides` (with expiry). `has_perm(perm, user)` precedence: override → system roles → department default → level default; probation/intern lose download/share_external/export. `effective_level(user)`.
- **Screen governance**: `screens` catalogue (key → path prefix), `screen_rules` (scope user|role|department|default, allow/deny, expiry), `effective_screens(user)` returns `{key,label,path,grp,allowed,source,sort_order}` with precedence individual override → role rule → system role → department rule → department default → company default → system default. `can_view_screen(key)`. The shell nav and `proxy.ts` must use it; **never rely only on hidden menu items**. `nav_layouts` (screen designer: nav order, home cards, quick actions) → `effective_nav(user)`; `module_usage`/`touch_module` for adoption.
- **Responsibilities register** (`responsibilities`: owner/backup/successor/escalation/SOP/decision_level/critical/RACI) + `who_owns(q)`; `glossary`; `delegations` (kinds approvals|leave|requests|decisions|tasks|help, expiry) → `effective_approver(user, kind)` reroutes approvals/leaves/requests automatically (delegate → or manager when away).
- **Access reviews** (`start_access_review`, `access_review_items` keep/remove/extend, `apply_access_review`), `access_findings()` (excess after transfer, stale grants, requester=approver conflicts, expiring, dormant), `org_health()` + nightly `org_completeness_audit` (missing managers/roles/leads/owners, unowned tasks/groups, critical without backup, probation overdue, contracts ending), `freeze_user/unfreeze_user/set_employee_status/freeze_department/lock_feature/feature_enabled`, `admin_sessions/revoke_session` + login alerts, `policies` (versions, targeting, `policy_acks`, quiz, `my_pending_policies`), `training_gate_ok` (org setting `security_training_course_id` gates confidential material for non-managers), `can_assign(assignee, assigner)` enforced by a task trigger (use Request Help otherwise), `assignment_warnings(assignee, due)`, delegation receipts `ack_task(task, accepted|clarify|conflict)`, `tasks.definition_of_done/quality_gates/reopen_reason/revision_count`, `import_people(rows)`, `config_history` + `undo_config(id)`, `reports_of(user)`, `change_impact(user, new_manager, new_department, new_role)`, `change_manager(user, new_manager, kind, reason)`.
- **Intelligence (0018)**: `commitments` (+`my_commitments()`, reminders cron), `standups` + `team_digest(day, dept)`, meeting hygiene (`meeting_load`, `meeting_cost`, focus windows in `departments.settings.focus_windows`), decisions `superseded_by/reversal_reason/status`, Q&A (`questions/answers/answer_votes`), `office_hours`/`expert_slots`, self-service `requests` (kinds expense|travel|purchase|wfh|field_duty|late_explanation|overtime|comp_off|training|other; routed to manager, second approver above threshold, SLA escalation), `resources/bookings` (no overlaps), `visitors`, `events/event_rsvps`, `service_status`, `broadcasts/broadcast_acks/checkins`, `allocations` + `capacity_calendar`, `surveys/survey_responses`, `retrospectives`, `experiments`, `what_if_absent(user, from, to)`, `waiting_on_me(user)`, `who_has_ball(type, id)`, `similar_tasks/similar_knowledge/similar_help_requests`, `while_you_were_away(since)`.
- Routes planned: `/requests`, `/status`, `/broadcasts/[id]`, `/policies/[id]`, `/wiki/questions`, `/workforce` (admin-only live attendance), `/admin/organization` (Organization Control), `/admin/people-intelligence`, `/connect` (GHL Connect).

## Phase 5b — Workforce admin (0019) & GHL Connect (0020)
- **0019 workforce admin**: `attendance_settings()` / `set_attendance_settings(jsonb)` (org.settings->attendance: grace, not-in alert, checkout reminder, auto checkout, short-day, early-leave, max break, idle hours, retention); `break_types` (lunch/tea/personal/prayer/errand with max minutes), `break_policies` (per department/shift: max_total_minutes, max_count, min_available, block_when_uncovered); `clock(p_kind,p_mode,p_note,p_location,p_source,p_break_type)` now enforces break policy + coverage (returns `coverage_warning`); `coverage_requirements` (dept, days, window, min_people, max_leave_same_day); RPCs `workforce_live()` (command center JSON: counts, departments via `department_coverage()`, open_breaks, not_in, missing_checkout, inactive, coverage_alerts), `coverage_forecast(from,to,dept)`, `leave_collisions(leave_id)`, `attendance_exceptions(from,to,dept)` (late/early_leave/missing_checkout/short_day/excess_break/absent), `attendance_patterns`, `my_attendance_exceptions` (self, Privacy Center), `attendance_summary`, `operational_inactivity(hours)` (clocked in but no task/message/attendance/focus activity — NEVER device telemetry), `can_see_workforce(dept)`; `access_events` + `log_access_event(kind,type,id,path,details)` (view/export/download/print/share/view_as/denied/search; self-readable; exports mirrored to security_events) + `access_event_summary(days)`; `people_intelligence(days)` (owner/super admin/hr.manage only; aggregated, explainable, no scores; logs its own access). Crons: `ghl_attendance_tick` */10 (not-in nudges → employee then manager, break exceeded, coverage gap, checkout reminder), `ghl_auto_checkout` 02:00 IST, `ghl_attendance_daily` 09:05 IST, `ghl_access_purge`.
- **0020 GHL Connect** (Sales/Support communication hub — NOT a CRM): `contacts` (work-relevant fields only, `vip`, `do_not_contact` enforced in DB), `inboxes` (shared email/phone/whatsapp/sms; `webhook_token`; assignment claim|round_robin|manual; SLA minutes) + `inbox_members` (agent|supervisor), `conversations` (status open|pending|snoozed|resolved|spam, assigned_to, sla_due_at/sla_breached, thread_key, disposition, linked task/help request), `conversation_messages` (direction inbound|outbound|internal; status draft|pending_approval|queued|sent|delivered|failed|received|rejected; `mentions` on internal notes), `calls`, `callbacks`, `follow_ups` (source manual|promise|call|ai|rule, `commitment_id`), `comm_templates` (email/whatsapp/sms/call_script; scope personal|department|company), `signatures`, `inbox_rules`. Permissions via `has_perm`: `connect.use/send/call/approve/manage/view_all` (seeded in role_defaults); `needs_send_approval(user)` (probation/intern or no connect.send → supervisor approval). RPCs: `contact_timeline(contact)`, `search_contacts(q)`, `start_conversation(contact,channel,subject,inbox)`, `queue_message(conversation, body, html, to[], cc[], subject, kind, template, ai, attachments)` → status queued|pending_approval, `approve_message(id, ok, note)`, `mark_message_sent(id, ok, external_id, error)` (called by `/api/connect/send` after the provider — Resend via `RESEND_API_KEY`, else mark failed "provider not configured"), `claim_conversation`, `assign_conversation`, `snooze_conversation`, `resolve_conversation(id, disposition, note)`, `log_call(contact, direction, phone, started, ended, disposition, notes, next_action, next_at, conversation, promise, promise_due)` (creates callback/follow-up/commitment; complaint|escalated → urgent + head notified), `add_promise`, `complete_follow_up`, `my_connect_queue()`, `connect_dashboard(inbox, days)`, `connect_settings()/set_connect_settings()`. Inbound: `ingest_email(token, payload)` / `ingest_message(token, payload)` (anon-callable, token per inbox) ← routes `/api/hooks/email/[token]`, `/api/hooks/message/[token]`. Cron `ghl_connect_sla` */10 (wake snoozed, SLA breach, unclaimed, callbacks/follow-ups/promises due). Realtime: conversations, conversation_messages, calls, follow_ups, callbacks. AI Email Buddy drafts are proposals (`ai_drafted`), never auto-sent; AI must not invent prices/guarantees.
- Routes: `/workforce` (Workforce Live, tabs live/exceptions/coverage/breaks/settings), `/admin/people-intelligence`, `/connect` (three-pane workspace: inbox list → conversation → contact/timeline; `/connect/[id]`, `/connect/follow-ups`, `/connect/contacts/[id]`, `/connect/dashboard`, `/connect/admin`).

## Phase 5 UI map (built 2026-09-08)
- **Screen governance (server-side)**: `src/proxy.ts` calls `effective_screens()` per governed request and redirects denied paths to `/no-access?screen=&from=` (page = explanation + `ScreenAccessRequest` which inserts `access_requests` with `resource_type='screen'`); `src/lib/screens.ts` (`matchScreen`, `isPathAllowed`), `useScreens()` in `SessionProvider` (`canOpen(path)`), `filterNav()` in `nav.ts`. Ungoverned paths: /no-access, /policies, /broadcasts, /status, /privacy, /profile, /settings. `PendingPoliciesBanner` is mounted in `(app)/layout.tsx`.
- **Employee control (G1)**: `/people/team` (tabs team/digest/capacity), `/admin/organization` (Organization Control: health/people/structure/roles/screens/permissions/policies/emergency/sessions/config/activity/audit; accepts `?propose=&user=&target=&reason=` from a Buddy `admin_action` and shows a review banner), `/policies/[id]`, `src/components/admin/{people,roles,screens}/*`, `TaskGovernance`, `DelegationCard`.
- **Workforce (G2)**: `/workforce` (`src/components/workforce/*`: Live, Exceptions, Coverage, Breaks, Summary, Settings), `/admin/people-intelligence`, admin `?tab=access-log`, break-type picker in `ClockWidget`, `MyExceptions` + access log in Privacy Center. Request-access fallback uses `resource_type='dataset'`.
- **GHL Connect (G3)**: `/connect` (workspace: `ConversationList` → `ConversationView` + `Composer` → `ContactPane`), `/connect/[id]`, `/connect/follow-ups`, `/connect/contacts` (+`/[id]`), `/connect/dashboard`, `/connect/admin`; `src/lib/connectPerms.ts`; API `POST /api/connect/send` (Resend via `RESEND_API_KEY`, else marks failed), `POST /api/hooks/email/[token]`, `POST /api/hooks/message/[token]`, `POST /api/ai/connect-draft`; `ConnectQueueCard` on home; ActivityProvider zone `conversation:<id>`.
- **Org intelligence (G4)**: `/requests`, `/status`, `/broadcasts/[id]` + `/broadcasts/new`, `/wiki/questions` (+`/[id]`), `/office-hours`, `/bookings`, `/visitors`, `/events`, `/pulse`, `/retros`, `/experiments`; components `commitments/MyCommitments`, `standups/Standup` (+`TeamDigest`), `meetings/MeetingHygiene`, `people/{CapacityCalendar,WhatIfAbsent,AllocationsCard}`, `leave/LeaveImpact`, `mywork/{WaitingOnMe,WhoHasBall}`, `home/WhileYouWereAway`, `intel/lib.ts` (JSON helpers, `useTouchModule`).
- **Buddy**: org tools (`org_who_reports_to`, `org_who_owns`, `org_what_if_absent`, `get_waiting_on_me`, `who_has_the_ball`, `get_my_attendance`, `get_my_commitments`; managers `org_health`, `org_change_impact`, `workforce_now`) and proposal kinds `commitment`, `request`, `admin_action` (hand-off only — AI never changes structure).

## GHL LIVE (schema 0021_live.sql)
- **What it is**: calls, huddles, meetings, screen share, whiteboards, recordings and live documents, wired into every entity page rather than living in a separate app. Media runs on LiveKit; all state is in Supabase.
- **Schema** (`supabase/migrations/0021_live.sql`): `live_rooms` (kind huddle|call|meeting|project_room|department_room|team_room|war_room|training|interview|virtual_office|temp|townhall|breakout|review|management|standup, linked to channel/project/task/department/team/help/incident/meeting, `persistent`, `visibility`, `locked`, `waiting_room`, `confidential`, `settings`), `live_participants`, `live_events`, `live_invites` (ring|knock|invite|request_share), `live_guest_links`, `live_notes`, `live_transcripts`, `live_polls`, `live_questions`, `live_recordings` + `live_recording_replies`, `boards`/`board_versions`/`board_comments`/`board_access_log`, `live_docs`/`live_doc_versions`/`live_doc_comments`, `collab_favorites`, `collab_policies` (permission matrix: default → department → role).
- **RPCs**: `start_live_room`, `join_live_room`, `leave_live_room`, `live_moderate`, `live_raise_hand`, `live_invite`, `live_invite_department`, `respond_live_invite`, `call_person`, `knock`, `live_room_task`, `live_room_decision`, `live_bookmark`, `live_running_late`, `create_breakouts`/`end_breakouts`, `live_guest_link`, `live_history(type,id)`, `board_snapshot`/`board_restore`/`board_element_task`/`board_to_project`, `live_doc_snapshot`, `live_doc_task`, `live_to_knowledge`, `huddle_suggestion(channel)`, `live_governance()`, `set_collab_policy`/`delete_collab_policy`, `collab_can(action)`, `set_live_state`, `live_tick()` (cron: idle rooms end, recordings expire by retention).
- **Client contract**: types in `src/lib/live/types.ts` (`CollabContext`, `CollabAction`, `StageMode`, `LiveDataMessage`, `BoardDoc`/`BoardOp`, `LiveDoc`, channel-name and storage-path helpers); browser helpers in `src/lib/live/client.ts`; global store in `src/components/live/liveStore.ts` (`openCollaborate(ctx, action)`, `useLive()`, `setActiveRoom`, `pushInvite`).
- **Routes**: `/live` (+ `/live/[id]`, guest `/live/guest/[token]`), `/boards` (+ `/boards/[id]`), `/recordings` (+ `/recordings/[id]`), `/docs` (+ `/docs/[id]`). All four are seeded in `screens`, so `filterNav()` and `proxy.ts` govern them like every other screen. Admin tab: `/admin?tab=live` (gated by `admin || communication.manage`).
- **Universal surface**: `<LiveProvider/>` + `<CollaborateMenu/>` are mounted in `AppShell` beside `<BuddyPanel/>`; `<CollaborateButton/>` sits in the header cluster. Entity pages drop `<EntityLive ctx={…}/>` next to `<BuddyQuickActions/>` (chat, project, task, department, help, incident, profile, meeting) and `<CollaborationHistory type= id=/>` for the memory panel. `<HuddleNudge channelId/>` sits above the chat composer. The floating mini call bar is `z-[95]`, `bottom: calc(56px + 12px + env(safe-area-inset-bottom))`, left of the Buddy FAB.
- **Realtime**: Supabase channels `live:<room>`, `board:<board>`, `doc:<doc>`, `live-invites:<user>`. Always `supabase.realtime.setAuth(access_token)` before `subscribe()` (RLS is evaluated with the token present at join time). `ActivityProvider` watches `live_rooms` / `live_invites` with zones `live:<id>` and `nav:/live`.
- **Live docs**: `live_docs.body` is markdown-ish text (serialised from a contentEditable by `htmlToMd`/`mdToHtml` in `src/components/docs/useLiveDoc.ts`), edited simultaneously over `doc:<id>` with presence, debounced ~700ms persistence and a version counter. Version history = `live_doc_versions` + `live_doc_snapshot()`; suggestion mode and comment threads = `live_doc_comments` (`suggestion`, `mentions`, `resolved`, `accepted`; the DB trigger sends mention notifications); highlight → task = `live_doc_task()`. `kind: meeting_notes` adds Record decision / Action item; breakout rooms get `kind: breakout_notes`.
- **Env**: `LIVEKIT_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET` (+ `NEXT_PUBLIC_LIVEKIT_URL`) — the token route reports `disabled` when they are missing, and the UI must degrade instead of crashing.
- **Recordings upload straight to Supabase Storage** (bucket `live`, `RECORDING_BUCKET`/`recordingPath()` in `src/lib/live/types.ts`) from the browser — never through a Next.js route, and never base64 through the database.
- **Rules**: administrators see live *metadata* only (`live_governance()`), never content. Confidential rooms cannot be recorded. Guest links expire. Every collaboration action is checked by `collab_can()` in the database — hiding a button is never the control.

## Multi-company platform (0022_platform.sql)
- **Hierarchy**: *platform* → *company (tenant)* → *department* → *team* → *person*. A tenant **is** an `organizations` row; `organizations` now carries `tenant_code`, `status` (trial|onboarding|active|suspended|read_only|archived|closed|pending), `plan`, `industry`, `country`, `timezone`, `email_domain`/`restrict_to_domain`, `custom_domain`/`domain_verified`, `logo_url`/`accent_color`/`welcome_message`, `terminology`, `limits`, `onboarding_stage`, `mfa_required`, `retention_days`.
- **Identity**: one person, many `memberships` (user_id + org_id + role + status). `active_workspace` holds the tenant they are currently in and `current_org()` resolves to it *only* when they have an active membership there or platform authority — otherwise to their profile's org. Switching is `set_active_workspace(p_org, p_reason)`; the picker list is `my_workspaces()`. **Never mix tenants in one view**: after switching, `router.refresh()` so every server component re-reads.
- **Platform roles** (`platform_admins.role`, `platform_role()`): `platform_super_admin` (owner — the only one who may edit `platform_admins`/`platform_assignments`/`platform_features`), `platform_ops` (create companies, change status), `platform_security`, `platform_support` / `platform_billing` / `platform_developer` (reach only the tenants listed in `platform_assignments`). `platform_can_touch(org)` is the gate; `platform_admin_invites` recognises a platform person by email before their first login. Platform staff are **not** members of any tenant.
- **Break-glass** (`start_break_glass` / `end_break_glass`): platform staff see administrative metadata by default; reading a tenant's *content* needs an open session with a written reason (≥12 chars), a justification category, a scope (`configuration`|`content`|`full`) and an expiry (5–480 min). Opening one notifies the company's own admins, writes `platform_audit_logs` + `security_events`, and shows a persistent banner in the shell until it is closed. The company's own door is `grant_support_access` / `revoke_support_access` (`/admin?tab=support`).
- **Company lifecycle**: `create_company(...)` (owner/ops only) → departments, channels, features, onboarding checklist, admin invite and a private onboarding room; `set_company_status(org, status, reason)` refuses `active` while `company_go_live_blockers(org)` is non-empty; `company_setup_health(org)` drives every progress bar. **Deletion is never offered** — only Suspend / Read-only / Archive, each with a typed reason.
- **Features**: catalogue in `platform_features`, per-tenant overrides in `org_features`. Read with `org_feature_enabled(key, org)`; write **only** through `set_org_feature(org, key, enabled, limit)` (audited).
- **RULE — every new table must carry `org_id` and an org-scoped policy** (`using (org_id = current_org() and …)`). A table that reaches its tenant only through a parent must be listed deliberately; `tenant_isolation_report()` flags anything missing and `tenant_isolation_probe(other_org)` (SECURITY INVOKER, runs under the caller's RLS) proves a second company reads zero rows. Both are the release check, surfaced at `/platform` → Isolation and on each company's Security tab.
- **UI**: `/platform` (command center), `/platform/[id]` (one company: Overview · Admins · Departments · Usage · Features · Security · Onboarding · Audit · Configuration), `/platform/new` (nine-step onboarding wizard). Components in `src/components/platform/*` (types + label maps in `lib.ts`). Every platform page re-checks `is_platform_admin()` server-side and `notFound()`s — hiding a nav item is never the control. `<WorkspaceSwitcher/>` in `AppShell` is the mandatory always-visible tenant indicator and turns orange (“Administrative mode”) when the viewer is in a company as platform staff.

## Web push + PWA (schema 0025_push.sql)
- **Why**: an in-app notification reaches nobody who is not already looking at a tab. Push is a *second* delivery channel — the `notifications` row stays the source of truth and every push failure is silent.
- **Flow**: any `insert into notifications` → AFTER INSERT trigger `notification_push_fanout()` → `net.http_post` to `push_config.hook_url || '/' || hook_secret` (same pg_net pattern as the webhook action in `0006_automation.sql`) → `POST /api/hooks/push/[token]` → `sendPushToUser()` → the browser's `/sw.js`.
- **Authentication — no service-role key.** Postgres calls the hook with no session, so the route authenticates with `PUSH_HOOK_SECRET` (constant-time compare, header or path) and every privileged read happens *inside Postgres*, in SECURITY DEFINER functions that re-check that same secret against `push_config.hook_secret`: `push_notification(secret, id)` (the row, as JSON), `push_targets(secret, user, org, kind)` (`{ok, quiet, subs}` — applies `in_quiet_hours()`, skipped for `kind in ('critical','action_required')`, and the org scoping, limit 25), `push_settle(secret, dead, shaky, delivered)` (prune / strike-out at 10 / stamp). Each returns a benign empty value on a wrong secret and never reveals whether a user or row exists, so `execute` is granted to `anon, authenticated` — the secret *is* the credential. The app itself runs on the anon key only; **do not reintroduce `SUPABASE_SERVICE_ROLE_KEY`** to "simplify" this.
- **Schema**: `push_subscriptions` (user_id, `org_id` + `push_subscription_org_default()` trigger mirroring `notification_org_default()`, unique endpoint, p256dh, auth, user_agent, last_used_at, failure_count; RLS = own rows for all operations + org-scoped SELECT for `is_admin()`/`security.manage`); `push_config` (one locked row: `hook_url`, `hook_secret`, `enabled` — **RLS on with no policies**, so a member can never select the secret). Writes go through `save_push_subscription()` / `remove_push_subscription()` (SECURITY DEFINER — a shared browser can hold another person's row on the same endpoint).
- **Tenant scoping**: a device belongs to the workspace it was registered in. `PushRegistrar` (mounted in `AppShell`) silently re-posts the subscription on every load so `org_id` follows a workspace switch, and the fan-out only targets subscriptions matching the notification's `org_id`. Company B never rings a device currently signed into company A.
- **Client**: `src/lib/push/client.ts` (`usePush()` → `{ ready, supported, permission, subscribed, blocked, subscribe, unsubscribe, message }`). **Permission is never requested on load** — only from a click. `blockReason()` distinguishes unsupported / iOS-without-install / denied / no-VAPID-key, each with a plain-language message in `BLOCK_MESSAGE`.
- **UI**: `PushOptInPrompt` (one dismissible line in `NotificationsPanel`, 30-day snooze in localStorage) and `PushOptIn` (the real toggle, in the profile's Notifications card next to `NotificationSettings`). Both in `src/components/notifications/PushOptIn.tsx`.
- **Service worker**: `public/sw.js` — push-only, no offline cache. A ring notification (`entity_type = 'live_room'` with `?invite=` in the link) gets `requireInteraction` plus **Accept** → `/live/<room>?invite=…&accept=1` and **Decline** → `POST /api/push/decline` (→ `respond_live_invite`). `pushsubscriptionchange` re-subscribes and re-posts. `sw.js` is excluded from the `proxy.ts` matcher; `/api/hooks/push/…` is already covered by the `/api/hooks/` anon allowlist.
- **Env**: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `PUSH_HOOK_SECRET`. Missing any of them degrades to "push not configured" / "no-secret" — nothing throws. **`SUPABASE_SERVICE_ROLE_KEY` is NOT required — nothing under `src/` reads it and the running app never holds one** (the only consumer is `scripts/verify.mjs`, the out-of-band DB self-test in `docs/TESTING.md`). A service key bypasses every RLS policy in the product, which is the opposite of what tenant isolation is for.
- **PWA**: `public/manifest.json` (standalone, shortcuts, PNG + maskable icons in `public/icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, `badge-96.png`). iOS delivers push only to an installed PWA — the opt-in says so instead of failing silently.

## Access control — Platform Owner, roles and permissions (schema 0035–0036)
- **The rule**: no permission = no access, and *admin is a role, not an amount of power*. `has_perm(perm, user)` is the single source of truth; `src/lib/permissions.ts` (`can`, `canAny`, `requirePermission`) and `usePermission`/`usePermissions`/`<Can>` both go through it. UI helpers hide controls — they are **never** the control. Every protected route, RPC and query is still checked in Postgres.
- **Precedence** (`has_perm`, highest first): platform owner reserved authority → active membership → **explicit individual DENY** → explicit individual ALLOW → employment status (probation/notice/suspended) → system roles → department default → level default → company super admin *bounded by `company_admin_limits`* → deny. The old first line was `if role = 'super_admin' then return true`, which made an explicit deny unwritable; individual rules are now evaluated first. Verified behaviour-identical against the previous logic across every user × permission (731 combinations, 0 mismatches) before anything was built on it.
- **Platform owner** = `platform_admins.role = 'platform_super_admin'`. `is_platform_owner()` / `is_platform_owner_user(uuid)`. Holds platform authority outright — the one unconditional branch, so the owner cannot lock themselves out. Keys prefixed `platform.` never resolve inside a company for anybody.
- **`permissions`** is the catalogue (key, label, description, `grp`, `risk` standard|elevated|high, `requires[]`, `platform_only`). Add a new key here or it will not appear in the studio, in `effective_permissions`, or in any risk/dependency warning.
- **`company_admin_limits`** caps what one company's own administrators may hold and hand out (`admin_ceiling(org)`; null = unrestricted, which is every existing company). Only the platform owner writes it; a company may read its own ceiling.
- **Nobody grants what they do not hold**: `can_manage_permission()` + BEFORE triggers on `permission_overrides`, `system_roles` and `role_defaults`. You must administer permissions, hold the key yourself, stay inside the ceiling, and have `security.manage` for high-risk keys; you cannot grant yourself anything. `guard_platform_admins` stops a tenant touching platform staff and stops the last owner deleting or demoting themselves. All authority changes land in `permission_changes` (before/after/actor).
- **Reads**: `explain_permission(user, perm)` returns the whole precedence chain (the "why can/why not" answer); `effective_permissions(user)`; `preview_user_access(user)` / `preview_role_access(role)` are **simulations, never impersonation**; `permission_holders(perm)` answers "who can see this?"; `role_change_impact(role, perms)` gives the diff, high-risk callout, dangling dependencies and affected-people count *before* a save; `access_review_board()` lists high-risk holders, temporary grants, acting roles and authority left on people who have gone. `system_role_versions` snapshots every role change and `restore_system_role()` rolls one back.
- **Guards must fail closed.** `can_review_access` first returned NULL for an unauthenticated caller (`p_user = auth.uid()` is NULL), and `if not NULL then raise` never fires — the guard fell open. Coalesce every guard to false. Equally: revoking an RPC from `anon` does nothing on its own, because the privilege comes from the PUBLIC grant a new function receives by default — `revoke … from public` **and** `from anon`, then grant `authenticated` back.
- **UI**: `/platform/access` (`src/components/access/*`) — Roles (action matrix + diff + impact + version history), People (effective access, per-key allow/deny/expiry, "Why?"), Permissions (catalogue + "who has this"), Company limits (owner only), Access review. Re-checked server-side and again inside every RPC.

## RBAC — granular permissions and the Company Super Admin (schema 0040–0042)
- **Module × action catalogue (0040)**: the catalogue was broad verbs (`view`, `create`, `edit`, `delete`) plus a few `<module>.manage` keys, which cannot express "may edit the wiki but not delete people". It is now `module.action` — `people.view`, `files.upload`, `announcements.publish`, `roles.assign` — 125 keys, of which 76 are granular. **Every granular key records the broad key it refines in `permissions.legacy_alias`, and `has_perm` resolves the key OR its alias.** A role written against `view` therefore still satisfies `people.view`, `wiki.view` and `files.view`: the split grants nothing new and takes nothing away. Verified behaviour-identical across every user × every pre-existing key (833 combinations, 0 mismatches) before anything was built on it. `permissions.module` / `.action` are informational; authorisation is always by key.
- **Precedence is unchanged**, and the exact key beats its alias: an explicit deny on `people.export` is not undone by an inherited allow on `export`. Employment-status restrictions (probation/intern lose download, share, export) follow the alias too, so `files.download` is blocked for the same people `download` always was.
- **Adding a key**: insert it into `permissions` with a `legacy_alias` pointing at the broad key that used to govern it, or null if it governs something genuinely new. The Access Studio's role matrix renders straight from the catalogue (`byGroup`), so a new key appears in the editor, in `effective_permissions`, and in the risk/dependency warnings with no front-end change.
- **`company_super_admin` (0041) is a security role, not a level.** "Runs this company" used to mean `profiles.role = 'super_admin'` — a hierarchy level, which is organisational identity. **Job title, hierarchy level and security role are three separate things and none of them may be derived from another**: `designation` is a job title and is display-only, `profiles.role` is where someone sits in the chain of command, and authority comes from `system_roles` held through `user_roles`. `is_company_super_admin()` is the check; it is bounded by `admin_ceiling()` exactly as the level branch is, and `platform.%` keys never resolve for it. Existing level-super-admins were backfilled once so nobody lost access; the two move independently from here. Never write `if role = 'super_admin'` (or anything reading `designation`) into an authorisation path — `test_access_control()` fails the build if `has_perm` ever mentions a job title.
- **Two guards that were missing (0041)**: `trg_guard_user_role` — nobody may *assign* a role granting a permission they do not hold themselves, nobody may give themselves a role, and a role containing a high-risk key needs `security.manage` to hand out. Deliberately narrower than `can_manage_permission()`, which asks "may you edit the catalogue" and would have blocked HR from making somebody an Employee. `trg_last_super_admin_profile` / `trg_last_super_admin_role` — a company can never be left with no administrator, counting both routes to the authority, refusing with the message the brief specifies.
- **Fixed in passing (0041)**: `log_permission_change()` read `new.perm` outside the branch that knows the column exists. plpgsql resolves a record field against the row's real composite type before the surrounding `CASE` picks a branch, so on `system_roles`, `role_defaults` and `company_admin_limits` it raised `record "new" has no field "perm"` — **creating a security role or saving a level default had been failing outright**. Read a record field only inside the branch whose table has it.
- **`access_control.view`** names the Access Control screen and refines `security.manage`, so a company can let somebody review who holds what without letting them change it. Both the nav item and the page gate accept either key; the page still re-checks server-side and every RPC re-checks in Postgres.
